import { Request, Response } from "express";
import { ItemPrice } from "@prisma/client";

export class itemPriceController {
  // This class can be used to handle item price related operations
  // For example, methods to get, update, or delete item prices can be added here
  // getItemsWithPrice(req: Request): Promise<Partial<ItemPrice[]>> {
  // }
}
